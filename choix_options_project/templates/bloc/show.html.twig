{% extends 'base.html.twig' %}

{% block title %}Bloc{% endblock %}

{% block body %}
    <h1>Bloc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bloc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bloc.name }}</td>
            </tr>
            <tr>
                <th>UeOptionalToChose</th>
                <td>{{ bloc.ueOptionalToChose }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bloc_index') }}">back to list</a>

    <a href="{{ path('app_bloc_edit', {'id': bloc.id}) }}">edit</a>

    {{ include('bloc/_delete_form.html.twig') }}
{% endblock %}
